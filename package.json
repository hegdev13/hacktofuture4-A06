{
  "name": "h24app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "dev:tokentap": "GOOGLE_GEMINI_BASE_URL=http://127.0.0.1:8080 npm run dev",
    "build": "next build",
    "start": "next start",
    "tokentap:start": "tokentap start --port 8080",
    "lint": "eslint",
    "train:rca-model": "python3 ai_agents/lightgbm_rca.py --mode train --output-dir ai_agents/models --rows 12000",
    "sim:load": "node scripts/push-cpu-pattern.cjs --profile high --duration 120 --interval 2",
    "sim:unload": "node scripts/push-cpu-pattern.cjs --profile low --duration 120 --interval 2",
    "sim:wave": "node scripts/push-cpu-pattern.cjs --profile wave --duration 180 --interval 2",
    "demo:self-heal": "./scripts/demo-self-healing.sh",
    "mock:server": "node mock-metrics-server.js",
    "dashboard": "node -e \"const http = require('http'); const fs = require('fs'); const path = require('path'); const PORT = process.env.PORT || 3000; const MIME = { '.html': 'text/html', '.js': 'application/javascript', '.css': 'text/css', '.json': 'application/json' }; const server = http.createServer((req, res) => { const file = req.url === '/' ? '/index.html' : req.url; const filePath = path.join(process.cwd(), file); const ext = path.extname(filePath); res.setHeader('Access-Control-Allow-Origin', '*'); if (!fs.existsSync(filePath)) { res.writeHead(404); res.end('Not found'); return; } const content = fs.readFileSync(filePath); res.setHeader('Content-Type', MIME[ext] || 'application/octet-stream'); res.writeHead(200); res.end(content); }); server.listen(PORT, () => console.log('Dashboard running at http://localhost:' + PORT));\"",
    "start:all": "node start-dashboard.js"
  },
  "dependencies": {
    "@elevenlabs/react": "^1.1.0",
    "@supabase/ssr": "^0.10.2",
    "@supabase/supabase-js": "^2.103.0",
    "clsx": "^2.1.1",
    "d3": "^7.9.0",
    "lucide-react": "^1.8.0",
    "next": "16.2.3",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "react-hot-toast": "^2.6.0",
    "recharts": "^3.8.1",
    "tailwind-merge": "^3.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/d3": "^7.4.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
